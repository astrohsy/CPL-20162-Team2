#include <iostream>
#include <Windows.h>

using namespace std;

int main()
{
	/*auto edsdkdll = LoadLibrary(TEXT("EDSDK.dll"));
	if (edsdkdll == nullptr) {
		cout << "로드 실패";
		return 0;
	}

	auto edsdklibdll = LoadLibrary(TEXT("EDSDKLib.dll"));
	if (edsdklibdll == nullptr) {
		cout << "로드 실패";
		return 0;
	}

	FreeLibrary(edsdkdll);
	FreeLibrary(edsdklibdll);*/
}